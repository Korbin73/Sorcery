defmodule IoHandlerTest do
  use ExUnit.Case
  doctest IoHandler

  test "Should send json to stdio" do
    
  end

  test "Should return ok from process" do
    
  end

  test "Should return ok from reading" do
    
  end
end
